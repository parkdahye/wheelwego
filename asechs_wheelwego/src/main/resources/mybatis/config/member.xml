<?xml version="1.0" encoding="UTF-8"?>
<!-- Sql Mapper -->
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="member"> 	
	<sql id="selectMember">
		select id,password,member_name,address,phonenumber,member_type from member
	</sql>
	  <resultMap type="memberVO" id="memberRM">
      <result property="memberName" column="member_name" />
      <result property="phoneNumber" column="phonenumber" />
	  <result property="memberType" column="member_type"/>
   </resultMap>
 	<select id="login" resultMap="memberRM" parameterType="memberVO">
 		select id,password,member_name,address,phonenumber,member_type from member
 		where id=#{id} and password=#{password}
 	</select>
 	 <select id="findMemberId" resultType="String" parameterType="memberVO">
      select id from member where member_name=#{memberName} and
      phonenumber=#{phoneNumber}
   </select>
   <select id="findMemberPassword" resultType="String" parameterType="memberVO">
      select password from member where id=#{id} and member_name=#{memberName} and phonenumber=#{phoneNumber}
   </select>
</mapper>
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
















